Standard TextInput with Label:

    <TextInput
     name="test"
     label="First Name"
     onChange={() => {}}
     placeholder="Enter first name" />

With validation error:

    <TextInput
     name="test"
     label="First Name"
     onChange={() => {}}
     error="First Name is requried." />
